document.addEventListener("DOMContentLoaded", function () {

    const johnLink = document.getElementById("Habeeb");

    johnLink.addEventListener("click", function (event) {
        event.preventDefault();
        const password = prompt ("Enter Password to view Habeeb's Result:");

        if (password === "habeeb003") {
            window.location.href = "Habeeb.html";
        } else {
            alert("wrong Password!");
        }

    })
})

document.addEventListener("DOMContentLoaded", function () {

    const johnLink = document.getElementById("Taoheed");

    johnLink.addEventListener("click", function (event) {
        event.preventDefault();
        const password = prompt ("Enter Password to view Taoheed's Result:");

        if (password === "taoheed57") {
            window.location.href = "Taoheed.html";
        } else {
            alert("wrong Password!");
        }

    })
})

document.addEventListener("DOMContentLoaded", function () {

    const johnLink = document.getElementById("Noheem");

    johnLink.addEventListener("click", function (event) {
        event.preventDefault();
        const password = prompt ("Enter Password to view Noheem's Result:");

        if (password === "noheem655") {
            window.location.href = "Noheem.html";
        } else {
            alert("wrong Password!");
        }

    })
})

document.addEventListener("DOMContentLoaded", function () {

    const johnLink = document.getElementById("Abdullah");

    johnLink.addEventListener("click", function (event) {
        event.preventDefault();
        const password = prompt ("Enter Password to view Abdullah's Result:");

        if (password === "abdullah787") {
            window.location.href = "Abdullah.html";
        } else {
            alert("wrong Password!");
        }

    })
})

document.addEventListener("DOMContentLoaded", function () {

    const johnLink = document.getElementById("Idris");

    johnLink.addEventListener("click", function (event) {
        event.preventDefault();
        const password = prompt ("Enter Password to view Idris's Result:");

        if (password === "idris545") {
            window.location.href = "Idris.html";
        } else {
            alert("wrong Password!");
        }

    })
})

document.addEventListener("DOMContentLoaded", function () {

    const johnLink = document.getElementById("Uthman");

    johnLink.addEventListener("click", function (event) {
        event.preventDefault();
        const password = prompt ("Enter Password to view Uthman's Result:");

        if (password === "uthman444") {
            window.location.href = "Uthman.html";
        } else {
            alert("wrong Password!");
        }

    })
})

document.addEventListener("DOMContentLoaded", function () {

    const johnLink = document.getElementById("Mahmud");

    johnLink.addEventListener("click", function (event) {
        event.preventDefault();
        const password = prompt ("Enter Password to view Mahmud's Result:");

        if (password === "mahmud456") {
            window.location.href = "Mahmud.html";
        } else {
            alert("wrong Password!");
        }

    })
})

document.addEventListener("DOMContentLoaded", function () {

    const johnLink = document.getElementById("Faruk");

    johnLink.addEventListener("click", function (event) {
        event.preventDefault();
        const password = prompt ("Enter Password to view Faruk's Result:");

        if (password === "faruk676") {
            window.location.href = "Faruk.html";
        } else {
            alert("wrong Password!");
        }

    })
})

document.addEventListener("DOMContentLoaded", function () {

    const johnLink = document.getElementById("Hassan");

    johnLink.addEventListener("click", function (event) {
        event.preventDefault();
        const password = prompt ("Enter Password to view Hassan's Result:");

        if (password === "hassan488") {
            window.location.href = "Hassan.html";
        } else {
            alert("wrong Password!");
        }

    })
})

document.addEventListener("DOMContentLoaded", function () {

    const johnLink = document.getElementById("Abdulqoyum");

    johnLink.addEventListener("click", function (event) {
        event.preventDefault();
        const password = prompt ("Enter Password to view Abdulqoyum's Result:");

        if (password === "abdulqoyum555") {
            window.location.href = "Abdulqoyum.html";
        } else {
            alert("wrong Password!");
        }

    })
})

document.addEventListener("DOMContentLoaded", function () {

    const johnLink = document.getElementById("Adeshina");

    johnLink.addEventListener("click", function (event) {
        event.preventDefault();
        const password = prompt ("Enter Password to view Abdullah's Result:");

        if (password === "adeshina567") {
            window.location.href = "Adeshina.html";
        } else {
            alert("wrong Password!");
        }

    })
})

document.addEventListener("DOMContentLoaded", function () {

    const johnLink = document.getElementById("Aishat");

    johnLink.addEventListener("click", function (event) {
        event.preventDefault();
        const password = prompt ("Enter Password to view Aishat's Result:");

        if (password === "aishat989") {
            window.location.href = "Aishat.html";
        } else {
            alert("wrong Password!");
        }

    })
})

document.addEventListener("DOMContentLoaded", function () {

    const johnLink = document.getElementById("Fareedah");

    johnLink.addEventListener("click", function (event) {
        event.preventDefault();
        const password = prompt ("Enter Password to view Ahmod's Result:");

        if (password === "fareedah006") {
            window.location.href = "Fareedah.html";
        } else {
            alert("wrong Password!");
        }

    })
})

document.addEventListener("DOMContentLoaded", function () {

    const johnLink = document.getElementById("Maryam");

    johnLink.addEventListener("click", function (event) {
        event.preventDefault();
        const password = prompt ("Enter Password to view Maryam's Result:");

        if (password === "maryam457") {
            window.location.href = "Maryam.html";
        } else {
            alert("wrong Password!");
        }

    })
})

document.addEventListener("DOMContentLoaded", function () {

    const johnLink = document.getElementById("Abdul-Rosheed");

    johnLink.addEventListener("click", function (event) {
        event.preventDefault();
        const password = prompt ("Enter Password to view Fareedah's Result:");

        if (password === "fareedah455") {
            window.location.href = "Abdul-Rosheed.html";
        } else {
            alert("wrong Password!");
        }

    })
})

document.addEventListener("DOMContentLoaded", function () {

    const johnLink = document.getElementById("Nusaybah");

    johnLink.addEventListener("click", function (event) {
        event.preventDefault();
        const password = prompt ("Enter Password to view Nusaybah's Result:");

        if (password === "nusaybah889") {
            window.location.href = "Nusaybah.html";
        } else {
            alert("wrong Password!");
        }

    })
})

document.addEventListener("DOMContentLoaded", function () {

    const johnLink = document.getElementById("Fareedah");

    johnLink.addEventListener("click", function (event) {
        event.preventDefault();
        const password = prompt ("Enter Password to view Fareedah's Result:");

        if (password === "fareedah467") {
            window.location.href = "Bello.html";
        } else {
            alert("wrong Password!");
        }

    })
})

document.addEventListener("DOMContentLoaded", function () {

    const johnLink = document.getElementById("Aliyah");

    johnLink.addEventListener("click", function (event) {
        event.preventDefault();
        const password = prompt ("Enter Password to view Aliyah's Result:");

        if (password === "aliyah333") {
            window.location.href = "Aliyah.html";
        } else {
            alert("wrong Password!");
        }

    })
})

document.addEventListener("DOMContentLoaded", function () {

    const johnLink = document.getElementById("Fatimoh");

    johnLink.addEventListener("click", function (event) {
        event.preventDefault();
        const password = prompt ("Enter Password to view Fatimoh's Result:");

        if (password === "fatimoh445") {
            window.location.href = "Fatimoh.html";
        } else {
            alert("wrong Password!");
        }

    })
})

document.addEventListener("DOMContentLoaded", function () {

    const johnLink = document.getElementById("Yahyah");

    johnLink.addEventListener("click", function (event) {
        event.preventDefault();
        const password = prompt ("Enter Password to view Maryam's Result:");

        if (password === "yahyah787") {
            window.location.href = "Yahyah.html";
        } else {
            alert("wrong Password!");
        }

    })
})

document.addEventListener("DOMContentLoaded", function () {

    const johnLink = document.getElementById("Oshodi");

    johnLink.addEventListener("click", function (event) {
        event.preventDefault();
        const password = prompt ("Enter Password to view Maryam's Result:");

        if (password === "oshodi676") {
            window.location.href = "Oshodi.html";
        } else {
            alert("wrong Password!");
        }

    })
})